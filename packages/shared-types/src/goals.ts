export type PaceStatus = "ahead" | "on_pace" | "behind" | "target_passed";
export type GoalStatus = "active" | "completed" | "archived";

export const MAX_NAME_LENGTH = 255;
export const MAX_NOTES_LENGTH = 500;
